{
  "name": "{{TOOL_NAME}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx vercel dev",
    "build": "tsc --noEmit",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@opensea/tool-sdk": "^{{SDK_VERSION}}",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "typescript": "6.0.2"
  }
}
